name: OnPrem

on:
  push:
    branches:
      - 'main'
      - 'dev'

jobs:
  build:
    runs-on: self-hosted
    steps:

    - name: 'Checkout'
      uses: 'actions/checkout@v4'

    - name: Run bash echo command
      run: echo Hello, world!